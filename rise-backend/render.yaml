services:
  - type: web
    name: rise-mastery-api
    env: docker
    branch: main
    plan: free
    region: oregon
    build:
      # Use the repository-root-relative path so Render finds the Dockerfile
      dockerfilePath: rise-backend/Dockerfile
      # Build context should be the backend folder
      context: rise-backend
