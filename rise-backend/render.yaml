services:
  - type: web
    name: rise-mastery-api
    env: docker
    branch: main
    plan: free
    region: oregon
    build:
      # Dockerfile path is relative to the build context; context is the backend
      dockerfilePath: Dockerfile
      context: rise-backend
